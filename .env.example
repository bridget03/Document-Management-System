# Copy sang ".env" ở cùng thư mục rồi điền giá trị thật. File .env KHÔNG commit.
# DB dùng chung cho service postgres + backend trong docker-compose.yml
POSTGRES_USER=dms_user
POSTGRES_PASSWORD=dat-mat-khau-manh-o-day
POSTGRES_DB=dms

JWT_SECRET=doi-secret-cua-ban-dai-va-ngau-nhien
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/api/google-drive/callback
# Fernet key mã hóa OAuth token Drive (bắt buộc ở production).
DRIVE_TOKEN_KEY=
